name: 'dilemme_prisonnier'
version: '1.0.0'
config-version: 2

profile: 'dilemme_prisonnier'

model-paths: ["models"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

vars:
  silver_dir: "../Data/Silver"

models:
  dilemme_prisonnier:
    staging:
      +materialized: view
    marts:
      +materialized: table
